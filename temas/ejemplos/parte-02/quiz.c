#include <stdbool.h> 
#include <stdio.h>
int main() 
{ 
bool a = true;
 bool b = false;
  if(a == 2)
    printf("Its ture");
  else if(b == 1)
    printf("B is fasle");
  else
    printf("Did not work!");
return 0; 
} 
